<?xml version="1.0" encoding="utf-8"?>
<!-- Launcher only: large inset shrinks the logo inside the adaptive-icon safe zone (circle crop). Login uses the PNG directly — not this drawable. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:bottom="28dp"
        android:left="28dp"
        android:right="28dp"
        android:top="28dp">
        <bitmap
            android:gravity="center"
            android:src="@drawable/vocalaid_brand_logo" />
    </item>
</layer-list>
